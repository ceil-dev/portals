import { createPortal } from './core';
import local from './local';
import client from './client';
import server from './server';

export default {
  createPortal,
  local,
  client,
  server,
};

export { createPortal, local, client, server };
